function criarDataset(field, constraint, sorFields) {
    totalTarefasAtrasadas = buscarTarefasAtrasadas();
    function buscarTarefasAtrasadas() {
        usuario = constraint;
        console.log(usuario);
        return 6;
    }
}

criarDataset("a", "b", "c");

//https://pt.stackoverflow.com/q/132156/101
