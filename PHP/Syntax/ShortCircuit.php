if ( $a() && $b() && $c() )

if ( $a() )
    if ( $b() )
        if ( $c() )
            fazAlgo;

if( $a() || $b() || $c() )

if ( $a() ) fazAlgo;
else if ( $b() ) fazAlgo; //faz algo igual
else if ( $c() ) fazAlgo; //tem que ser igual

//https://pt.stackoverflow.com/q/154056/101
